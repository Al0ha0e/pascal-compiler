program example(input, output);
    var q, w: array[1..2,27..40] of integer;x,y:integer;m,n:real; ##${dsada
    sda}
    function gcd(var a, b: integer): integer;
        begin 
            while x < 10 do
            begin
                x := x + 1;
            end;
        end;
    begin
        read(q[1,27], y);
        write(gcd(x, y))
    end.
